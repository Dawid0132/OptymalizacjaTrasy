server:
  port: 8080
spring:
  application:
    name: tsp_rest_api
  datasource:
    url: jdbc:mysql://localhost:3306/SmartRoute
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update